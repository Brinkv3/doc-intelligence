# --- Core pipeline ---
llm-adapter[anthropic] @ git+https://github.com/Brinkv3/llm-adapter.git  # Provider-agnostic LLM calls
PyMuPDF                # PDF parsing (import as fitz)
python-docx            # DOCX parsing
pandas                 # CSV / tabular handling
tiktoken               # Token counting

# --- Dev / test ---
pytest                 # Test harness
